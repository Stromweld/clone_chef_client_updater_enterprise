---
driver:
  name: exec
  # name: proxy
  # host: localhost
  # reset_command: "exit 0"
  # port: <%= ENV["machine_port"] %>
  # username: <%= ENV["machine_user"] %>
  # password: <%= ENV["machine_pass"] %>

transport:
  name: exec

platforms:
  - name: localhost
  - name: windows-latest
